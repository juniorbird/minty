var foo = 'bar';

var anObj = { d: 'one', e: 'two' };

var anArr = [ 'three', 'four', 'five' ];

var myObj = { a: 1,
            b: 2,
            c: false };

var someonesRegex = /[abc]/g;

var annoyingFunction = function (foo) {
  var bar = '[';
  return foo + bar;
}

var templateString = `this is
                      a multiline [
                      template string`;

var singleLineTemplateString = `amazing { string`;

var moreAnnoyingArrowFunction = (baz) => baz + '[' + "{";

if (life = 'good') {
  console.log('yay!');
} else{
  console.log("hugs");
}

var fuckedString = "an open brace looks like {  while an opening bracket looks like [";

var fuckedMultilineString = "I am doing this on [so many" +
  " lines mother{fucker";

var yourArr = [
  'foo',
  'baz',
  'quux'
];

var yourObj = {
  z: 10,
  w: 9,
  x: 8,
};
